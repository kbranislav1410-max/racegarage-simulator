# Database URL for PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/racegarage?schema=public"

# Email Configuration
# Provider: "resend", "nodemailer", or leave empty for console fallback
EMAIL_PROVIDER="resend"

# Resend Configuration (if using EMAIL_PROVIDER="resend")
RESEND_API_KEY="re_RT43o48W_9ztziFQWzbszcLEhKNMMVtoh"

# Nodemailer Configuration (if using EMAIL_PROVIDER="nodemailer")
SMTP_HOST=""
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""

# Email "From" address (used by both providers)
EMAIL_FROM="info@racegarage.sk"
